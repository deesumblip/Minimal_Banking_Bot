action_endpoint:
  actions_module: "actions"

nlg:
  type: rephrase
  llm:
    model_group: ollama-llm

model_groups:
  - id: ollama-llm
    models:
      - provider: ollama
        model: llama3.2
        api_base: "http://localhost:11434/v1"
        temperature: 0.3
