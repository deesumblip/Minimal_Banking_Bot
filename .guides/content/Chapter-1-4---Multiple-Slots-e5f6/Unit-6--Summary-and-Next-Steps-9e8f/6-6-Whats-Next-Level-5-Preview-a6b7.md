**Starting point:** **`level4/`** (see **Unit 0.1**).

**Level 5** adds **tool calling** on the **same** banking project in **`level5/`**: a **`tools/`** module, registration in **`endpoints.yml`**, and flows/actions that let the LLM invoke tools when a single fixed branch is not enough.

The full baseline recap, **`SearchReady`** / prompt setup, lab table, and checklist are in **Level 5 Unit 0** (**0-1** and **0-2**). Read those next—this page does not duplicate that handoff.
