In Level 6 you implemented the lab sequence in **Unit 0.2**’s **delta checklist** (sub-agent config, **`mcp_servers`**, **`ask_banking_assistant`** flow, train, test). Use **0-2** and the lab pages for the numbered file list—this page only states the **idea**.

**Orchestration:** the main agent runs a flow that **`call:`**s the **banking_assistant** sub-agent; the sub-agent runs until completion, then control returns to the main dialogue.
