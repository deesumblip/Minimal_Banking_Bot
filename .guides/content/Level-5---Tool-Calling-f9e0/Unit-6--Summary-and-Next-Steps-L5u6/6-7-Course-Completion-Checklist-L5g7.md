**Starting point:** **`level5/`** with every lab in this chapter complete.

You should be able to:

- Contrast **tools** (model-chosen at runtime) with **actions** (named in the flow).
- Add **`prompt_template`** and the command prompt under **`data/prompts/`** (**Lab 2.0**).
- Build **`tools/`**, export functions in **`__all__`**, and register **`tools:`** in **`endpoints.yml`** (**Labs 2.1–3.1**).
- Connect **`transfer_money_tools`**, **`from_llm`** conditions, and an action that reads slots and may invoke tools (**Lab 4.1**).
- Train from **`level5/`** and pass **Lab 5.2**.

**Next:** **Level 6 Unit 0** walks through **`level6/`** and sub-agent labs. Open it when you are ready for orchestration.
