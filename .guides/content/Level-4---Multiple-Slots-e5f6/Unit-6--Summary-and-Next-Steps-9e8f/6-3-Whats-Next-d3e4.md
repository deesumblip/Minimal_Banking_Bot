**Starting point:** **`level4/`** (see **Unit 0.1**).

Optional directions after Level 4 include stronger validation, extra flows, NLU tuning, and channels—all still trained from **`level4/`**.

**Tool calling** (LLM-chosen functions) is the next **chapter-level** step: **Level 5 Unit 0** explains motivation, the **`level5/`** baseline, and labs—read that when you are ready, instead of a second preview here.
