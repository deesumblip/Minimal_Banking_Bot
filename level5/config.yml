# Level 5 main assistant: SearchReady command generator and FlowPolicy, with `assistant_id: level5-agent` so your trained models stay separate from Level 4.

recipe: default.v1
language: en
assistant_id: level5-agent

pipeline:
  - name: SearchReadyLLMCommandGenerator
    llm:
      model_group: openai-gpt-5-1

policies:
  - name: FlowPolicy
