To let users talk to the banking assistant, add a **flow** whose **steps** include **`call: banking_assistant`**.

## Lab 4.1

Create **`level6/data/basics/ask_banking_assistant.yml`** (not in the starter repo). Under **`flows:`**, define your flow id, **`name`**, **`description`**, and **steps** including **`call: banking_assistant`** (matching **`agent.name`**) and optionally **`action: utter_help`**. Write **`name`** / **`description`** yourself for your course.

When done, run the Lab 4.1 assessment.
